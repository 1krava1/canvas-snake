class NodeList {
    /**
     * @param {Node[]} nodeFrom
     */
    constructor(nodes){
        this.nodes = nodes;
    }

    /**
     * @param {Node} node
     * @return {Node[]}
     */
    isNodeExist( node ) {
    }

    /**
     * @param {Node} nodeFrom
     * @param {Node} nodeTo
     * @return {Node[]}
     */
    getNodesFromTo( nodeFrom, nodeTo ) {
    }
}